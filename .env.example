# 应用配置
NODE_ENV=production
PORT=3000

# 数据库配置（SQLite不需要额外配置）
# 数据存储在 /app/data/weight_tracker.db

# 安全配置
# JWT_SECRET=your-secret-key  # 如果将来添加JWT认证

# 备份配置
# BACKUP_INTERVAL=86400000  # 自动备份间隔（毫秒）

# 日志配置
# LOG_LEVEL=info
# LOG_FILE=/app/logs/app.log

# 服务器配置
# MAX_UPLOAD_SIZE=10mb
# CORS_ORIGIN=*

# 邮件配置（可选，用于通知）
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=user@example.com
# SMTP_PASS=password
# SMTP_FROM=Weight Tracker <noreply@example.com>